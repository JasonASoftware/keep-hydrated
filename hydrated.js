// to round it to the smallest intenger you would have to use math.floor function
function litres(time) {
  // math.floor function returns a intenger that is less than or equal to a given number
  return Math.floor(time * 0.5);
}
